---
phase: 3.2
title: Revenue Logic & Distribution Hardening
---

# Phase 3.2: Revenue Logic & Distribution Hardening

## Goal
Complete automated revenue distribution cascade, finalize the demo "Simulate Incoming Funds" logic, and seamlessly integrate the revenue DB simulation with the dashboard logic.

## Tasks

```xml
<task id="3.2.1" type="execute" autonomous="true">
  <description>Refactor DistributeRevenuePanel Simulation Logic</description>
  <read_first>
    - src/app/components/DistributeRevenuePanel.tsx
    - src/app/hooks/useRevenueContract.js
    - src/app/api/revenue/route.ts
  </read_first>
  <action>
    Modify `DistributeRevenuePanel.tsx` and `useRevenueContract.js`:
    - Remove the hardcoded manual distribution loop from `sendRevenueDemoMode`.
    - Change `sendRevenueDemoMode` to simulate an incoming payment that triggers an API call to record the payment and automatically calculate the percentage splits for the `rightsHolders` based on the newly deposited funds.
    - Instead of just setting `txStatus`, make a POST request to a new `/api/distribution` or `/api/revenue/simulate` endpoint.
  </action>
  <acceptance_criteria>
    - `sendRevenueDemoMode` in `useRevenueContract.js` sends a POST request with the `distributeAmount`.
    - `DistributeRevenuePanel.tsx` updates its state properly after the simulated funds arrive.
  </acceptance_criteria>
</task>

<task id="3.2.2" type="execute" autonomous="true">
  <description>Implement Automated Distribution DB API</description>
  <read_first>
    - src/app/api/revenue/route.ts
    - src/app/lib/database.ts
  </read_first>
  <action>
    Create or update `src/app/api/revenue/route.ts` to handle POST requests for incoming funds:
    - Receive `projectId` and `amount`.
    - Fetch the project's rights holders from `project_contributors` using `getProjectContributors`.
    - Call `recordPayment` (which already automatically distributes to contributors based on their `revenue_share`).
    - Ensure it returns the success status so the UI can update.
  </action>
  <acceptance_criteria>
    - `src/app/api/revenue/route.ts` handles POST requests.
    - Successfully calls `recordPayment` in `database.ts` using the provided amount.
  </acceptance_criteria>
</task>

<task id="3.2.3" type="execute" autonomous="true">
  <description>Dashboard Data Synchronization</description>
  <read_first>
    - src/app/dashboard/admin/page.tsx
    - src/app/hooks/useSplits.ts
  </read_first>
  <action>
    - Ensure that when the "Simulate Incoming Funds" finishes, an event `payment-recorded` is dispatched.
    - Verify `useSplits.ts` listens to `payment-recorded` and re-fetches DB revenue.
    - Ensure Admin Dashboard updates real-time by re-fetching projects or triggering a global state update.
  </action>
  <acceptance_criteria>
    - Global window event `payment-recorded` is dispatched upon successful simulation.
    - The dashboard state automatically refreshes when this event is fired.
  </acceptance_criteria>
</task>
```
