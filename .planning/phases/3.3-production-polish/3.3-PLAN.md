---
phase: 3.3
title: Production Polish & Verification
---

# Phase 3.3: Production Polish & Verification

## Goal
Polish the visual output for granular splits (rights holders UI) and conduct comprehensive build testing and E2E verification to assure production readiness.

## Tasks

```xml
<task id="3.3.1" type="execute" autonomous="true">
  <description>Refine Granular Split Aesthetics</description>
  <read_first>
    - src/app/components/RightsHolderCard.tsx
    - src/app/dashboard/admin/roster/page.tsx
  </read_first>
  <action>
    - Update `RightsHolderCard.tsx` to ensure that it has zero visual jank or clipping. Adjust paddings and gradient stops to ensure text readability across dark and light aesthetics.
    - Polish the "Live Split Preview" UI in `DistributeRevenuePanel.tsx` to match the premium dark mode glassmorphism of the application, ensuring micro-animations are smooth when percentages update.
  </action>
  <acceptance_criteria>
    - Visual layout modifications are present.
    - Component renders without TS errors.
  </acceptance_criteria>
</task>

<task id="3.3.2" type="execute" autonomous="true">
  <description>Run Comprehensive Build & Lint Checks</description>
  <read_first>
    - package.json
  </read_first>
  <action>
    - Run the Next.js production build (`npm run build`).
    - Run the linter (`npm run lint`).
    - Resolve any lingering warnings or errors that would prevent Vercel from deploying successfully.
  </action>
  <acceptance_criteria>
    - `npm run build` exits with a `0` code.
    - `npm run lint` exits cleanly.
  </acceptance_criteria>
</task>

<task id="3.3.3" type="execute" autonomous="true">
  <description>End-to-End Environment Verification</description>
  <read_first>
    - src/lib/supabaseClient.ts
    - src/lib/web3/client.ts
  </read_first>
  <action>
    - Add a tiny verification health-check logic in the top level Layout or via a `scripts/verify-env.js` script to ensure that the environment variables (like `NEXT_PUBLIC_SUPABASE_URL`) are present.
    - Run the E2E health check script to confirm.
  </action>
  <acceptance_criteria>
    - A script or health-check confirms required runtime configs exist.
    - The terminal outputs a successful "System Ready" message.
  </acceptance_criteria>
</task>
```
