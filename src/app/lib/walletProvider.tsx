'use client';

import React from 'react';

/**
 * Minimal WalletProvider stub.
 * Replace with real wallet/wagmi logic later.
 */
export function WalletProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}